import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    marginTop: 20,
  },
  notFound: {
    marginTop: 30,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
